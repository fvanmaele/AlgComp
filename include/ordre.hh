#ifndef ORDER_HH
#define ORDER_HH
#include <list>
#include <vector>

template <typename T> // T::operator<
void order_linear_extension(const std::vector<T> &X) {
    // recursively: choose minimal element(s), remove from set

}

#endif